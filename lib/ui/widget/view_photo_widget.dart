
import 'package:flutter/material.dart';

class ViewPhotoWidget extends StatelessWidget {
  const ViewPhotoWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}