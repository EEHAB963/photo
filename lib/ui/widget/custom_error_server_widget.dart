import 'package:flutter/material.dart';

class CustomErrorServerWidget extends StatelessWidget {
  const CustomErrorServerWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return  const Center(
            child: Icon(Icons.dangerous),
          );
  }
}